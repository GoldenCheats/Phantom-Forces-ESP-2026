-- [[ PF Spectrum - STABLE LOADER + CLOSE BUTTON ]] --

local CoreGui = game:GetService("CoreGui")

-- Cleanup existing UI
if CoreGui:FindFirstChild("SpectrumUI") then
    CoreGui.SpectrumUI:Destroy()
end

-- Main Container
local SpectrumUI = Instance.new("ScreenGui")
SpectrumUI.Name = "SpectrumUI"
SpectrumUI.Parent = CoreGui
SpectrumUI.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Main Window
local Main = Instance.new("Frame")
Main.Name = "Main"
Main.Parent = SpectrumUI
Main.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
Main.Position = UDim2.new(0.5, -125, 0.5, -75)
Main.Size = UDim2.new(0, 250, 0, 160)
Main.Active = true
Main.Draggable = true 

local MainCorner = Instance.new("UICorner", Main)
MainCorner.CornerRadius = UDim.new(0, 10)

-- Header / Title
local Title = Instance.new("TextLabel")
Title.Parent = Main
Title.Size = UDim2.new(1, -40, 0, 40)
Title.Position = UDim2.new(0, 10, 0, 0)
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.GothamBold
Title.Text = "PF SPECTRUM"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 16
Title.TextXAlignment = Enum.TextXAlignment.Left

-- [[ CLOSE BUTTON ]] --
local CloseBtn = Instance.new("TextButton")
CloseBtn.Name = "CloseBtn"
CloseBtn.Parent = Main
CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 60, 60)
CloseBtn.BackgroundTransparency = 0.8
CloseBtn.Position = UDim2.new(1, -30, 0, 8)
CloseBtn.Size = UDim2.new(0, 22, 0, 22)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 14
CloseBtn.AutoButtonColor = true

local CloseCorner = Instance.new("UICorner", CloseBtn)
CloseCorner.CornerRadius = UDim.new(0, 5)

CloseBtn.MouseButton1Click:Connect(function()
    SpectrumUI:Destroy()
end)

-- Execution Logic
local function SafeExecute(url)
    local success, response = pcall(function()
        local code = game:HttpGet(url)
        local func, err = loadstring(code)
        if func then
            task.spawn(func)
            return true
        else
            return false, err
        end
    end)
    if not success then warn("Execution Error: " .. tostring(response)) end
end

-- Button Builder
local function MakeBtn(text, pos, url)
    local btn = Instance.new("TextButton")
    btn.Parent = Main
    btn.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
    btn.Position = UDim2.new(0.1, 0, 0, pos)
    btn.Size = UDim2.new(0.8, 0, 0, 38)
    btn.Font = Enum.Font.GothamSemibold
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(240, 240, 240)
    btn.TextSize = 13
    
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    
    btn.MouseButton1Click:Connect(function()
        btn.Text = "LOADING..."
        btn.BackgroundColor3 = Color3.fromRGB(0, 120, 215)
        SafeExecute(url)
        task.wait(0.5)
        btn.Text = text
        btn.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
    end)
end

-- Add your scripts
MakeBtn("MAIN ESP", 55, "https://raw.githubusercontent.com/GoldenCheats/Phantom-Forces-ESP-2026/refs/heads/main/main%20esp")
MakeBtn("TRASH ESP", 100, "https://raw.githubusercontent.com/GoldenCheats/Phantom-Forces-ESP-2026/refs/heads/main/External%20ESP")

print("Spectrum UI Loaded with Close Button.")
